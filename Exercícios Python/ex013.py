salario = float(input('Digite o salario do funcionario R$: '))
salario_reajustado = salario + (salario * 0.15)
print('O salário do funcionário com reajuste de 15% é de {:.2f}'.format(salario_reajustado))